import { DateTime } from 'luxon';
import { UserOut } from '../../utils/dtos/User';
import { prisma, sql } from '../../utils/prisma';

export async function getPopularSongs(numberOfSongs: string) {
  try {
  } catch (error) {
    throw error;
  }
}
